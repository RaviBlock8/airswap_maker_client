import { BigNumber } from 'ethers/utils';
import { Quote } from '@airswap/types';
export declare function createQuote({ signer, sender }: {
    signer?: {};
    sender?: {};
}): Quote;
export declare function isValidQuote(quote: Quote): boolean;
export declare function getTotalBySignerAmount(quotes: Array<Quote>): BigNumber;
export declare function getTotalBySenderAmount(quotes: Array<Quote>): BigNumber;
