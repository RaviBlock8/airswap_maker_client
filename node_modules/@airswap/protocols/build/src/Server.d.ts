import { BigNumber } from 'ethers/utils';
import { Quote, Order } from '@airswap/types';
export declare class Server {
    private client;
    constructor(locator: string);
    getMaxQuote(signerToken: string, senderToken: string): Promise<Quote>;
    getSignerSideQuote(senderAmount: string, signerToken: string, senderToken: string): Promise<Quote>;
    getSenderSideQuote(signerAmount: string, signerToken: string, senderToken: string): Promise<Quote>;
    getSignerSideOrder(senderAmount: string, signerToken: string, senderToken: string, senderWallet: string): Promise<Order>;
    getSenderSideOrder(signerAmount: string | BigNumber, signerToken: string, senderToken: string, senderWallet: string): Promise<Order>;
    private compare;
    private generateRequest;
}
