import { ethers } from 'ethers';
import { BigNumber } from 'ethers/utils';
export declare class ERC20 {
    chainId: string;
    address: string;
    private contract;
    constructor(address: string, chainId?: string, walletOrProvider?: ethers.Wallet | ethers.providers.Provider);
    balanceOf(address: string): Promise<BigNumber>;
}
