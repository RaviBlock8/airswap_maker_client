import { ethers } from 'ethers';
import { Order } from '@airswap/types';
export declare class Validator {
    chainId: string;
    private contract;
    constructor(chainId?: string, walletOrProvider?: ethers.Wallet | ethers.providers.Provider);
    static getReason(reason: string): string;
    static getAddress(chainId?: string): string;
    checkSwap(order: Order): Promise<Array<string>>;
    checkWrappedSwap(order: Order, fromAddress: string, wrapperAddress: string): Promise<Array<string>>;
    checkDelegate(order: Order, delegateAddress: string): Promise<Array<string>>;
    checkWrappedDelegate(order: Order, delegateAddress: string, wrapperAddress: string): Promise<Array<string>>;
    private convertToArray;
}
