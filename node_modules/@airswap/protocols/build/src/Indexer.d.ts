import { ethers } from 'ethers';
import { LocatorResult } from '@airswap/types';
export declare class Indexer {
    chainId: string;
    private contract;
    constructor(chainId?: string, walletOrProvider?: ethers.Wallet | ethers.providers.Provider);
    static getAddress(chainId?: string): string;
    getLocators(signerToken: string, senderToken: string, protocol?: string, limit?: number, cursor?: string): Promise<LocatorResult>;
}
