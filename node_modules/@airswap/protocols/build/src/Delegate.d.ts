import { ethers } from 'ethers';
import { Quote, Order } from '@airswap/types';
export declare class Delegate {
    chainId: string;
    address: string;
    private tradeWallet;
    private contract;
    constructor(address: string, chainId?: string, walletOrProvider?: ethers.Wallet | ethers.providers.Provider);
    getWallet(): Promise<string>;
    getMaxQuote(signerToken: string, senderToken: string): Promise<Quote>;
    getSignerSideQuote(senderAmount: string, senderToken: string, signerToken: string): Promise<Quote>;
    getSenderSideQuote(signerAmount: string, signerToken: string, senderToken: string): Promise<Quote>;
    provideOrder(order: Order, wallet?: ethers.Wallet): Promise<string>;
    private getQuotedOrMaxAvailable;
}
